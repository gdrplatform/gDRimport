# Check the structure of raw data

Check the structure of raw data

## Usage

``` r
.check_file_structure(
  df,
  filename,
  sheet_name,
  readout_offset,
  n_row,
  n_col,
  bcode_idx,
  bcode_col
)
```

## Value

`NULL` invisibly.
