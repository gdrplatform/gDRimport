# Get Excel sheets

get sheets for given set of XLS files

## Usage

``` r
get_xl_sheets(files)
```

## Arguments

- files:

  charvec with file paths

## Value

named list where names are the excel filenames and the values are the
sheets within each file
