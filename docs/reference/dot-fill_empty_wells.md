# Correct plates with not fully filled readout values

Correct plates with not fully filled readout values

## Usage

``` r
.fill_empty_wells(
  df,
  plate_rows,
  data_rows,
  exp_row,
  exp_col,
  numeric_regex = "^\\d+$"
)
```

## Value

data.table with corrected plates data
